# dev-memory

**Description:** 项目级 AI 记忆系统（v2.0）。三层混合分层架构（项目级索引 + 会话级摘要 + 消息级 JSONL 记录），寄生式注入到各工具最高优先级层保障触发可靠性。双触发机制（开头检查 + 结尾保存），自动识别会话涉及的项目（支持多项目、跨项目），分诊过滤低价值对话，分层读取控制 token 成本，增量写入避免全量重写，提取六维开发记忆（任务摘要、踩坑记录、探索路径、最终结论、架构决策、环境信息），按项目归属分别写入对应项目目录的 Markdown 记忆文件。消息级 JSONL 必选记录（intent/actions/outcome/learned），会话级 MD 摘要提供人类可读索引，USER_PROFILE.md 维护用户画像 + 贡献者日志 + 设备溯源。取消按周归档，旧记忆保留不删，溢出条目以 `[~]` 标记由 Dream 整理合并。新记忆以时间戳、价值标记和引用计数标注，对旧记忆中的错误/矛盾/过时之处进行更正和澄清。执行流程 7.5 步，支持时效性检测、会话去重和自我进化。

**Details:**

# dev-memory — 项目级 AI 记忆系统

## 核心原则（12条）

1. **双触发**：开头检查建立保存意识，结尾触发执行保存，不依赖用户说特定的话
2. **分诊优先**：不值得保存的对话直接跳过，避免无效 IO
3. **分层读取**：按场景读取记忆文件的不同部分，控制 token 成本
4. **增量写入**：只输出新条目和插入位置，不重写未变化的部分
5. **追加式演进**：旧记忆永不删除，只追加更正说明
6. **价值标记**：每条记忆标注价值等级，结合引用计数反映实际使用情况
7. **纯 Markdown + JSONL**：所有记忆以 Markdown（人类可读）+ JSONL（机器可读）存储，不执行任何脚本，不依赖任何运行时
8. **时间精确**：所有时间戳精确到分钟，格式 `[YYYY-MM-DD HH:MM]`
9. **自我进化**：执行中发现不足时自动改进本 SKILL.md，含触发可靠性审计
10. **多项目感知**：自动识别会话涉及的项目，按项目归属分发记忆
11. **三层混合分层**：项目级索引（DEV_MEMORY.md，策展层）+ 会话级摘要（sessions/YYYY-MM-DD.md，导航层）+ 消息级记录（sessions/YYYY-MM-DD.jsonl，原始层）。横向补充 USER_PROFILE.md（跨会话用户画像 + 贡献者溯源）和 CONFIG.md（配置 + 工具注入指引）
12. **寄生式注入**：将触发规则以"薄 Rule"形式注入到各编程工具（TRAE/Claude Code/Cursor/Codex/Windsurf/Cline/Copilot 等）的最高优先级层（Rules/CLAUDE.md/.cursor/rules/AGENTS.md/.windsurf/rules/.clinerules/copilot-instructions.md），厚 Skill（本 SKILL.md）按需加载完整执行流程。薄 Rule 只写"何时触发 + 触发什么"，厚 Skill 写完整执行规范

## 双触发机制

### 开头检查（每轮对话首次响应时）

在每轮对话的**首次响应**时，AI 内部静默执行以下检查（不打扰用户）：

1. **项目扫描**：扫描当前 workspace 下的项目目录，检查哪些有/没有 `dev-memory/DEV_MEMORY.md`
2. **首次初始化标记**：如果项目没有记忆文件 → 在内部标记"本轮对话结束前需初始化"
3. **时效性检查**：如果项目记忆文件存在，读取其 metadata 行（`> 最后更新: ...`）和"当前状态概览"段落（约 150 token），检查：
   - 距上次更新是否 >1 天 → 标记"本轮需更新"
   - 各维度最新条目时间戳，如某维度 >14 天未更新 → 标记"该维度可能过时"
4. **会话去重检查**：对比 metadata 中的 `会话: <session_id>` 与当前 session_id：
   - 如果相同且无新任务完成 → 本轮跳过保存
   - 如果相同但有新任务完成 → 继续保存
   - 如果不同 → 正常保存
5. **设备检查（v2.0 新增）**：读取 `dev-memory/USER_PROFILE.md` 的"设备历史"区块：
   - 获取当前设备标识（Windows: `%COMPUTERNAME%`；macOS/Linux: `hostname`）和开发者名（`%USERNAME%` / `$USER`）
   - 如果当前设备不在设备历史列表中 → 在内部标记"本轮需追加新设备条目"
   - 如果当前设备已在列表中 → 更新"最后使用"时间为今天
6. **sessions/ 目录检查（v2.0 新增）**：检查 `dev-memory/sessions/` 目录是否存在：
   - 不存在 → 在内部标记"本轮需创建 sessions/ 目录"
   - 存在 → 检查当日文件 `sessions/YYYY-MM-DD.jsonl` 和 `sessions/YYYY-MM-DD.md` 是否存在，不存在则标记"本轮需创建当日文件"

**开头检查的成本控制**：只读取 metadata 行 + 状态概览 + USER_PROFILE.md 设备历史区块（约 150 token），不读全量记忆文件。

### 结尾触发（满足任一即触发保存流程）

| 触发条件 | 说明 | 强度 |
|---------|------|------|
| **话题切换** | 用户消息开启了明显不同于上一个任务的新话题 | 强制 |
| **任务完成** | 多步骤任务到达完成点（代码已推送/文件已创建/测试通过等） | 强制 |
| **显式请求** | 用户说"保存记忆""记录"等 | 强制 |
| **对话深度安全网** | 对话已超过 10 轮交互且未触发过任何记忆保存 | 主动提议 |
| **收尾语** | "好的""可以了""谢谢"等 | 弱触发 |

## 保存流程

```
开头检查（静默，Rule 注入保障）
  ↓
结尾触发（任务完成/话题切换/显式请求/安全网）
  ↓
Step 0: 多项目识别与记忆分发
  ↓
Step 1: 分诊 — 值得保存？→ 不值得 → 跳过，不输出反馈
  ↓
Step 2: 读取现有记忆（分层读取）
  ↓
Step 3: 提取本次会话六维记忆
  ↓
Step 4: 对比检查 — 更正与澄清 + 引用计数更新
  ↓
Step 5: 写入主文件 + 追加 JSONL + 更新会话摘要
  ├─ 5.1 写入 DEV_MEMORY.md
  ├─ 5.2 追加 sessions/YYYY-MM-DD.jsonl
  ├─ 5.3 更新 sessions/YYYY-MM-DD.md
  └─ 5.4 更新 USER_PROFILE.md
  ↓
Step 6: 自我进化检查
  ↓
Step 6.5: 轻量 Dream 整理（条件触发）
  ↓
Step 7: 可见反馈
```

## 执行流程（7.5步）

### Step 0: 多项目识别与记忆分发

一次会话可能涉及 workspace 内的多个项目。本步骤自动识别会话内容涉及哪些项目，并将后续提取的记忆按项目归属分发。

#### 0.1 扫描 workspace 项目结构

1. 确定当前 workspace 根目录（通常为 TRAE 打开的工作区文件夹）
2. 扫描 workspace 下的一级和二级子目录，识别项目目录。识别标志（满足任一即可）：
   - 含有 `dev-memory/DEV_MEMORY.md`（已有 dev-memory 记忆的项目）
   - 含有 `.git/` 目录（Git 仓库）
   - 含有 `package.json`、`requirements.txt`、`pyproject.toml`、`Cargo.toml`、`go.mod`、`pom.xml` 等项目标识文件
   - 含有 `README.md` 且目录名非通用名（非 `docs`、`assets`、`node_modules` 等）
3. 构建**项目清单**：`{ 项目路径, 项目名称, 是否已有dev-memory }`

#### 0.2 分析会话内容的项目归属

逐段分析本次会话的对话上下文，为每段内容标注所属项目。识别线索（按优先级排序）：

| 优先级 | 线索类型 | 示例 |
|--------|---------|------|
| 高 | 文件路径引用 | `D:\trae\workspace\project-a\src\app.py` → project-a |
| 高 | 项目名称提及 | "在 dev-memory 项目里加个功能" → dev-memory |
| 中 | 代码/配置特征 | 特定 package.json 的依赖名、数据库路径、API 端点 |
| 中 | 已有记忆关联 | 对话内容与某项目 DEV_MEMORY.md 中的条目高度相关 |
| 低 | 当前工作目录 | 如果无明确线索，归属到当前工作目录所在项目 |

**跨项目内容处理规则：**
- 如果一段内容同时涉及多个项目（如"把 A 项目的接口给 B 项目用"），在该段记忆中标注关联项目，并**在每个相关项目的 DEV_MEMORY.md 中各写入一份**，在条目末尾标注 `[关联项目: project-b]`
- 如果一段内容无法明确归属任何项目，归入当前工作目录的项目；如果当前工作目录不在任何已知项目内，创建/更新 workspace 根目录下的 `dev-memory/DEV_MEMORY.md` 作为**通用记忆**
- 如果一次会话只涉及一个项目，直接进入 Step 1，跳过分发逻辑

#### 0.3 生成分发计划

根据 0.2 的分析结果，生成**分发计划**：

```
分发计划示例：
├─ project-a (D:\trae\workspace\project-a)
│   ├─ 任务摘要: [本次会话中关于 project-a 的内容]
│   ├─ 踩坑记录: [project-a 相关的坑]
│   └─ ...其他维度
├─ project-b (D:\trae\workspace\project-b)
│   ├─ 任务摘要: [本次会话中关于 project-b 的内容]
│   └─ ...其他维度
└─ 通用 (workspace 根目录) [可选]
    └─ [无法归属的内容]
```

后续 Step 1~7 对**每个涉及的项目**分别执行一遍。

#### 0.4 新项目自动纳入

如果会话中涉及一个尚无 `dev-memory/` 的项目目录：
1. 在该项目的 `dev-memory/` 下创建 `DEV_MEMORY.md`（使用首次初始化模板）
2. 创建 `dev-memory/sessions/` 子目录
3. 创建 `dev-memory/USER_PROFILE.md`（使用 USER_PROFILE.md 模板）
4. 在项目清单中标记为"新纳入"

### Step 1: 分诊 — 值得保存吗？

快速评估本次会话是否值得保存记忆（约 200 token 成本，避免对低价值对话执行完整流程）。

**值得保存（满足任一）：**
- 产生了新的代码/文件/配置变更
- 解决了一个错误或技术问题
- 做了架构或技术选型决策
- 发现了新的 API/工具/环境信息
- 探索了多个方案并得出结论

**不值得保存：**
- 纯概念问答（"什么是 REST API"）
- 单纯的文件读取/查看
- 闲聊或项目讨论未落地

**分诊原则：宁可多存不可漏存。** 如果不确定，按"值得保存"处理。

如果不值得保存 → 跳过后续所有步骤，不输出可见反馈，不写入 JSONL。

### Step 2: 读取现有记忆（分层读取）

根据当前操作读取记忆文件的不同层级：

| 场景 | 读取范围 | 估计 token |
|------|---------|-----------|
| 开头检查 | DEV_MEMORY.md metadata + 状态概览 + USER_PROFILE.md 设备历史 | ~150 |
| Step 4 对比检查 | 各维度最新 1 条 + 历史更正记录 | ~500 |
| Step 2~6 完整执行 | 全量 DEV_MEMORY.md + 最近 1 天 sessions/MD | ~2500 |

1. 读取 `项目/dev-memory/DEV_MEMORY.md`
   - 如果文件不存在，跳到"首次初始化特殊规则"
2. 读取本 SKILL.md 中的"迭代记录"区域（用于 Step 6 自我进化检查）
3. 读取 `dev-memory/sessions/YYYY-MM-DD.md`（当日会话摘要，如存在），了解当日已记录的会话
4. **时效性扫描**：扫描各维度最新条目时间戳，如某维度 >14 天未更新，标记该维度"可能过时"，在 Step 5 写入时在该维度标题下追加 `⚠️ 最后更新: YYYY-MM-DD（N天前），可能已过时`
5. **引用计数 token 更新**：读取各条目的 `[引用:N]` 标记，供 Step 4 引用计数更新使用

### Step 3: 提取本次会话六维记忆

从对话上下文中提取以下六个维度的记忆：

| 维度 | 说明 | 格式 |
|------|------|------|
| ① 任务与变更摘要 | 本次做了什么（1-3句话） | `- [YYYY-MM-DD HH:MM] [★级别] 描述 [引用:0]` |
| ② 踩坑记录 | 遇到什么错误、根因、修复方式 | `- [YYYY-MM-DD HH:MM] [★级别] ✅/❌ 错误描述 → 原因: ... → 修正: ... [引用:0]` |
| ③ 探索路径 | 尝试了什么方案、为什么放弃/采纳 | `- [YYYY-MM-DD HH:MM] [★级别] 方案A(结果) → 方案B(结果) → 最终采纳C(原因) [引用:0]` |
| ④ 最终结论与最佳实践 | 当前验证有效的结论 | `- [YYYY-MM-DD HH:MM] [★级别] 结论描述 [引用:0]` |
| ⑤ 架构决策记录 | 为什么选这个方案、放弃什么、影响范围 | `- [YYYY-MM-DD HH:MM] [★级别] 决策描述 → 原因: ... [引用:0]` |
| ⑥ 环境与依赖信息 | 工具版本、API变更、配置变更 | `- [YYYY-MM-DD HH:MM] [★级别] 信息描述 [引用:0]` |

提取规则：
- 如果某个维度本次会话没有新内容，跳过该维度，不强制填充
- 每条记忆必须以 `[YYYY-MM-DD HH:MM]` 时间戳开头
- 时间戳使用当前系统时间（北京时间）
- 踩坑记录标注状态：✅ 已解决 / ❌ 未解决
- **每条新记忆末尾必须带 `[引用:0]` 初始标记**（v2.0 新增），后续在 Step 4 和 Dream 整理中递增

**价值标记规则**（每条记忆必须标注）：
- `★★★` 架构决策、重要踩坑、关键技术结论
- `★★` 任务完成、方案选择、环境变更
- `★` 次要变更、文案修改、小修复

### Step 4: 对比检查 — 更正与澄清 + 引用计数更新

逐条将新提取的记忆与主文件 DEV_MEMORY.md 中已有的记忆进行对比：

1. **发现矛盾**：新旧记忆对同一事物有不同描述
   → 在"历史更正记录"区块追加：`- [YYYY-MM-DD HH:MM 更正] 旧记忆"原文摘要" → 更正: 新说明`
2. **发现过时**：旧记忆中的信息已不再适用
   → 在"历史更正记录"区块追加：`- [YYYY-MM-DD HH:MM 过时] 旧记忆"原文摘要" → 过时原因: ... → 当前状态: ...`
3. **发现错误**：旧记忆中的信息本身有误
   → 在"历史更正记录"区块追加：`- [YYYY-MM-DD HH:MM 更正] 旧记忆"原文摘要" → 更正: 正确信息`

**时效性关联检查**：如果新记忆与 >14 天的旧记忆相关，主动检查旧记忆是否仍然有效，如已过时则追加更正。

**引用计数更新（v2.0 新增）**：
- 逐条检查新提取的记忆是否引用了 DEV_MEMORY.md 中已有的条目（通过内容关联、references 字段、维度关联判断）
- 被引用的已有条目，其 `[引用:N]` 计数 +1，更新为 `[引用:N+1]`
- 引用计数递增的触发场景：
  - 新记忆的六维提取中明确引用了该条记忆（如"基于 ⑤ 条目决策..."）
  - 新记忆的对比检查中发现与该条记忆相关（如更正、澄清、延续）
  - 新记忆的 references 字段中列出了该条记忆
- 在 Step 5.1 写入时，同步更新被引用条目的 `[引用:N]` 标记

**关键约束：旧记忆本身不修改、不删除，只追加更正说明。引用计数的更新是例外——仅修改 `[引用:N]` 标记数字，不改动条目正文。**

### Step 5: 写入主文件 + 追加 JSONL + 更新会话摘要

本步骤分为 4 个子步骤，按顺序执行。

#### 5.1 写入 DEV_MEMORY.md（增量写入）

对 `dev-memory/DEV_MEMORY.md` 执行以下操作。**使用增量插入，不重写未变化的部分。**

1. **更新顶部 metadata**：使用 SearchReplace 替换 metadata 行
   ```
   > 最后更新: YYYY-MM-DD HH:MM | 会话: <session_id> | 指纹: 6维/N条/M更正/分数 | 最后整理: YYYY-MM-DD
   ```
   - 移除了 `周归档: YYYY-Www`（v2.0 取消归档制）
   - 新增 `最后整理: YYYY-MM-DD`（记录上次 Dream 整理日期）

2. **更新"当前状态概览"**：使用 SearchReplace 替换该段落为新的 2-3 句话概括

3. **增量插入新条目**：对每个有新内容的维度，使用 SearchReplace 在对应维度的 `###` 标题下方、第一条已有条目上方插入新条目
   - 搜索锚点：`### N. 维度名称\n`（如 `### 1. 任务与变更摘要\n`）
   - 替换为：`### N. 维度名称\n\n- [新条目] [引用:0]\n[原有第一条条目]`
   - 每条新条目末尾必须带 `[引用:0]` 初始标记
   - 如果该维度本次无新内容，跳过

4. **更新引用计数**（v2.0 新增）：对 Step 4 中识别的被引用条目，使用 SearchReplace 将 `[引用:N]` 更新为 `[引用:N+1]`

5. **追加历史更正记录**（如果有）：在"历史更正记录"区块顶部追加

6. **追加时效性警告**（如果 Step 2 标记了过时维度）：在对应维度标题下追加 `⚠️ 最后更新: YYYY-MM-DD（N天前），可能已过时`

7. **溢出标记**（如果某维度条目超出上限，见"溢出处理"章节）：在超出的旧条目前标记 `[~]`

**关键约束：只输出新条目和插入位置，不重复输出未变化的旧内容。**

#### 5.2 追加 sessions/YYYY-MM-DD.jsonl（v2.0 新增，必选）

生成一条 JSONL 记录，追加到 `dev-memory/sessions/YYYY-MM-DD.jsonl` 文件末尾。每条消息一个 JSON 对象，一行一条。

**JSONL Schema（9 个字段）**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `message_id` | string | 是 | 消息唯一标识（取 session_id 前 12 位 + 序号，或使用消息 hash） |
| `timestamp` | string | 是 | 精确到分钟，格式 `YYYY-MM-DD HH:MM` |
| `session_id` | string | 是 | 会话标识（与 DEV_MEMORY.md metadata 中的 session_id 一致） |
| `intent` | string | 是 | 用户意图/请求（1 句话概括） |
| `actions` | string[] | 是 | AI 执行的动作列表（每个动作为一个字符串元素） |
| `outcome` | string | 是 | 结果（1-2 句话） |
| `learned` | string[] | 否 | 学到的新知识/经验（每个知识点一个元素） |
| `dimensions` | string[] | 否 | 关联的六维维度编号（①~⑥） |
| `references` | string[] | 否 | 引用的 DEV_MEMORY.md 已有条目（格式：`"DEV_MEMORY.md ⑤条目: 条目摘要"`） |

**完整 JSON 示例**：

```json
{"message_id":"6a55a2eef0e6","timestamp":"2026-07-14 10:52","session_id":"6a55a2eef0e645bb36d55adf","intent":"用户要求创建公开仓库","actions":["检查私有仓库记忆文件状态","创建 orphan 分支","推送至 dev-memory-public"],"outcome":"公开仓库创建成功，私有仓库保留记忆历史","learned":["orphan 分支可以创建无历史记录的干净初始提交","git rm --cached 可取消已跟踪文件的追踪"],"dimensions":["①","⑤"],"references":["DEV_MEMORY.md ⑤条目: 隐私感知Git策略"]}
```

**写入规则**：
- 如果当日文件 `dev-memory/sessions/YYYY-MM-DD.jsonl` 不存在，创建新文件
- 追加到文件末尾，一行一条，不以逗号分隔
- 每次"任务完成"硬触发时追加一条
- 每次"话题切换"硬触发时追加一条
- 分诊跳过时不写入
- `learned`、`dimensions`、`references` 为空时使用空数组 `[]`，不可省略字段

#### 5.3 更新 sessions/YYYY-MM-DD.md（v2.0 新增）

在当日会话摘要文件 `dev-memory/sessions/YYYY-MM-DD.md` 中追加本次会话的摘要。

**格式模板**：

```markdown
# 会话摘要 — YYYY-MM-DD

## HH:MM — <一句话标题> [session: <session_id>]
<2-4 句话摘要，概括本次会话做了什么、得出什么结论>
[续: <上次相关会话的日期和标题>]
```

**设计要点**：
- 每个会话摘要 2-4 句话
- 包含 session_id 便于关联 JSONL
- `[续: ...]` 标注跨会话连续性，链接到上一次相关会话（查找 sessions/ 目录中最近的会话摘要，或 DEV_MEMORY.md 中的相关条目）
- 按时间正序排列（最早在上，最新在下）
- 如果当日文件不存在，创建新文件并写入头部 `# 会话摘要 — YYYY-MM-DD`
- 如果当日文件已存在，在文件末尾追加新的 `## HH:MM — ...` 区块

#### 5.4 更新 USER_PROFILE.md（v2.0 新增）

更新 `dev-memory/USER_PROFILE.md`：

1. **更新贡献者日志**：在"贡献者日志"表格末尾追加一行：
   ```
   | YYYY-MM-DD HH:MM | <开发者名> | <设备标识> | <session_id> | <本次变更摘要> |
   ```

2. **更新设备历史**：
   - 如果当前设备已在列表中 → 更新该设备条目的"最后使用"时间为今天
   - 如果当前设备不在列表中 → 追加新设备条目（见 USER_PROFILE.md 模板）

3. **更新开发者偏好**（如果本次会话发现了新的用户偏好）：
   - 如用户表达了新的工作风格、命名习惯、质量要求等，更新"当前开发者偏好"区块

### Step 6: 自我进化检查（含审计）

回顾本次执行过程，检查以下方面：

#### 6a. 触发可靠性审计

- **触发来源**：本次执行是因为什么触发的？
  - 硬触发（任务完成/话题切换）→ 正常
  - 显式请求（用户手动要求）→ ⚠️ 自动触发可能失效，记录并改进触发机制
  - 安全网（10轮未保存）→ ⚠️ 前面的硬触发未生效，检查原因
- **记忆缺口检测**：检查 DEV_MEMORY.md 上次更新时间。如果距本次 >2 天且中间有开发活动（可通过 Git log 或文件修改时间判断），在"历史更正记录"中追加：
  ```
  - [YYYY-MM-DD HH:MM 缺口] 上次更新 YYYY-MM-DD，距今 N 天，期间开发活动可能未记录
  ```
- **首次初始化遗漏检测**：如果执行时发现项目没有 DEV_MEMORY.md 且项目已存在多日（通过 .git 目录或文件修改时间判断），说明首次初始化机制失效，在迭代记录中记录
- **寄生式注入审计（v2.0 新增）**：如果本次执行是因为"显式请求"或"安全网"触发的，检查对应项目的 Rule 注入是否生效（CONFIG.md 中记录的注入状态），如失效则在迭代记录中标注

#### 6b. 记忆质量检查

- **a) 维度不足**：六维记忆无法覆盖某些重要信息
  → 在 SKILL.md 中新增维度说明，并在迭代记录中追加
- **b) 溢出处理边界**：遇到溢出标记 `[~]` 逻辑未覆盖的边界情况
  → 在 SKILL.md 中补充规则，并在迭代记录中追加
- **c) 提取规则不清**：某些信息经常遗漏或提取不准
  → 在 SKILL.md 中优化提取规则描述，并在迭代记录中追加
- **d) JSONL/MD 验证（v2.0 新增）**：
  - 检查 `dev-memory/sessions/YYYY-MM-DD.jsonl` 是否成功追加（文件存在且非空，最后一行是合法 JSON）
  - 检查 `dev-memory/sessions/YYYY-MM-DD.md` 是否成功更新（文件存在且包含本次 session_id）
  - 检查 `dev-memory/USER_PROFILE.md` 的贡献者日志是否成功追加
  - 如任一检查失败，在迭代记录中记录失败原因并补充规则
- **e) 其他改进点**

如有改进，在本 SKILL.md 的"迭代记录"区域追加：
```markdown
### vX.X — [YYYY-MM-DD HH:MM] 改进说明
- 改进内容：...
- 触发原因：...
- 影响范围：...
```

如无改进，跳过。

### Step 6.5: 轻量 Dream 整理（记忆巩固）

借鉴 Claude Code autoDream 的三个核心操作，但在正常保存流程中内联执行，不需要 daemon 常驻。

**触发条件**（满足任一）：
- DEV_MEMORY.md 全维度条目总数 >24 条（v2.0 调整，因上限放宽，原 20 条）
- 距上次 Dream 整理 >7 天（在 metadata 中记录 `最后整理: YYYY-MM-DD`）
- 历史更正记录 >5 条

**如果不满足触发条件，跳过本步骤。**

**五个轻量操作**：

| 操作 | 说明 | 示例 |
|------|------|------|
| ① 合并 | 跨维度找关联条目，添加交叉引用 | 踩坑记录中的 API 参数 → 关联到最终结论中的最佳实践，追加 `[关联: ④条目摘要]` |
| ② 去重 | 找近似重复条目，保留更详细的，合并到一条 | 两条都记"代理端口 9674"→ 合并为一条，标注合并来源 |
| ③ 提升 | ★ 条目被后续多条记忆验证 → 升级为 ★★ 或 ★★★ | 某 ★ 条目"API 正确参数"被后续 3 次引用验证 → 升级为 ★★★ |
| ④ 引用频率提升（v2.0 新增） | 根据引用计数动态提升价值等级 | `★` 条目被引用 ≥3 次 → 提升为 `★★`；`★★` 条目被引用 ≥5 次 → 提升为 `★★★`；`★★★` 条目 >30 天未被引用 → 标记 `[⚠️ 低引用]`，提示可能过时 |
| ⑤ 跨层合并（v2.0 新增） | 扫描 sessions/JSONL 中的 `learned` 字段，提升高频知识到项目级索引 | 如果某个 `learned` 被多次独立记录（≥2 次出现在不同 session 的 JSONL 中）→ 提升为 DEV_MEMORY.md 的 ④ 最终结论，标注 `[跨层提升: sessions/YYYY-MM-DD.jsonl]` |

**溢出条目优先整理**：
- 扫描所有标记 `[~]` 的溢出条目
- 优先对溢出条目执行合并/去重操作
- 如果溢出条目长期未被引用（>30 天），在标记前追加 `[~⚠️]`，提示"已溢出且低引用，详情见 sessions/"

**与 Claude Code Dream 的关键区别**：
- 不需要 daemon 常驻，不需要持续 API 调用
- 在 Step 5 之后内联执行
- 所有操作有可见反馈
- 只整理不删除（合并的旧条目标注 `[已合并→新条目]`，不物理删除）

**执行后**：
- 更新 metadata 中的 `最后整理: YYYY-MM-DD`
- 在 Step 7 可见反馈中标注"含 Dream 整理"

### Step 7: 可见反馈（含健康度评分与记忆指纹）

记忆保存完成后，**必须**在回复末尾向用户输出简短确认。

**健康度评分**（0-100，每次保存时计算）：

| 维度 | 计算方式 | 权重 |
|------|---------|------|
| 新鲜度 | 距上次更新天数：<1天=满分，>7天=0 | 30% |
| 覆盖度 | 有内容的维度数 / 6 | 25% |
| 密度 | 最近7天条目数 / 预期条目数 | 20% |
| 准确度 | 1 - (更正记录数 / 总条目数) | 15% |
| 价值分布 | ★★★条目占比 | 10% |

**记忆指纹**（更新到 metadata 行）：
```
> 最后更新: YYYY-MM-DD HH:MM | 会话: <id> | 指纹: 6维/N条/M更正/分数 | 最后整理: YYYY-MM-DD
```

**输出格式**：

```
📝 已更新项目记忆: <项目名> (<N>条新记忆, <M>条更正)[, 含 Dream 整理]
   健康度: 85/100 (新鲜✓ 覆盖✓ 密度⚠ 准确✓ 价值✓)
   会话记录: sessions/YYYY-MM-DD.jsonl (+1条), sessions/YYYY-MM-DD.md (+1段)
   → dev-memory/DEV_MEMORY.md
```

首次初始化：
```
📝 已初始化项目记忆: <项目名> (基于本次会话[ + TRAE 内置记忆迁移])
   用户画像: dev-memory/USER_PROFILE.md (已创建)
   会话记录: sessions/YYYY-MM-DD.jsonl (+1条), sessions/YYYY-MM-DD.md (+1段)
   → dev-memory/DEV_MEMORY.md
```

分诊跳过（不值得保存）：不输出任何反馈。

## 文件结构（v2.0）

每个项目目录下维护以下文件：

```
项目根目录/
  dev-memory/                    ← 直接放在项目根目录（v2.0 从 docs/ 移到根目录）
    CONFIG.md                    ← 配置文件（安装引导 + 隐私设置 + 工具注入指引，始终纳入 Git）
    DEV_MEMORY.md                ← 项目级六维记忆索引（策展层，默认 gitignore）
    USER_PROFILE.md              ← 项目级用户画像 + 贡献者日志 + 设备历史（默认 gitignore）
    SKILL.md                     ← 完整执行规范（可选本地副本，有下载地址则不需要）
    sessions/                    ← 会话级 + 消息级记录（默认 gitignore）
      YYYY-MM-DD.md              ← 当日会话摘要（Markdown，人类可读，导航层）
      YYYY-MM-DD.jsonl           ← 当日消息级记录（JSONL，机器可读，原始层，必选）
```

- 如果 `dev-memory/` 目录不存在，自动创建
- 如果 `dev-memory/sessions/` 子目录不存在，自动创建
- CONFIG.md 始终纳入 Git（不含记忆数据，仅含配置和引导）
- DEV_MEMORY.md、USER_PROFILE.md、sessions/ 默认 gitignore（隐私保护），用户可手动取消
- **v2.0 取消 `archive/` 目录**：不再按周归档，溢出条目以 `[~]` 标记保留在主文件中，由 Dream 整理合并

## 隐私感知初始化

首次初始化时，检测 Git 仓库可见性并给出引导：

1. 检查 `.git/config` 中 remote URL 是否包含 github.com/gitlab.com 等
2. 如果是公开仓库 → 记忆文件默认 gitignore（推荐），仅本地维护
3. 如果是私有仓库 → 建议纳入 Git，便于多设备同步
4. 在 CONFIG.md 中记录隐私设置选择
5. 输出引导信息：
   ```
   📋 记忆隐私策略: [公开仓库→记忆文件已忽略 / 私有仓库→建议追踪]
      如需修改，编辑 .gitignore 中的 dev-memory 部分
   ```

**.gitignore 模板**（v2.0 路径更新）：
```gitignore
# === dev-memory 隐私策略 ===
# 默认不追踪记忆文件，保护开发隐私
# 如需追踪（如私有仓库便于多设备同步），注释掉以下行
dev-memory/DEV_MEMORY.md
dev-memory/USER_PROFILE.md
dev-memory/sessions/

# 始终追踪的文件（配置和引导，不含记忆数据）
!dev-memory/CONFIG.md
!dev-memory/SKILL.md
```

## DEV_MEMORY.md 模板（v2.0）

首次初始化时使用以下模板：

```markdown
# 项目开发记忆

> 最后更新: YYYY-MM-DD HH:MM | 会话: <session_id> | 指纹: 6维/N条/M更正/分数 | 最后整理: YYYY-MM-DD

## 当前状态概览
（2-3句话描述项目当前进展和最新关键变更）

## 六维记忆索引

### 1. 任务与变更摘要
（最近8条，按时间倒序，带价值标记和引用计数）

### 2. 踩坑记录
（最近8条，按时间倒序，标注 ✅已解决 / ❌未解决，带价值标记和引用计数）

### 3. 探索路径
（最近5条，记录尝试了什么方案、为什么放弃/采纳，带价值标记和引用计数）

### 4. 最终结论与最佳实践
（当前有效的结论，发现更好的会追加更正，带价值标记和引用计数）

### 5. 架构决策记录
（最近5条，记录为什么选这个方案，带价值标记和引用计数）

### 6. 环境与依赖信息
（最近8条，工具版本、API变更、配置变更，带价值标记和引用计数）

## 历史更正记录
（对旧记忆中错误/矛盾/过时之处的更正，保留原文+更正说明）
```

**v2.0 模板变更说明**：
- metadata 移除 `周归档: YYYY-Www`，新增 `最后整理: YYYY-MM-DD`
- 移除"归档索引"区块
- 溢出上限调整：任务摘要/踩坑记录 5→8 条，探索路径/架构决策 3→5 条，最终结论/环境信息 5→8 条
- 每条条目末尾带 `[引用:0]` 初始标记

## USER_PROFILE.md 模板（v2.0 新增）

首次初始化时创建 `dev-memory/USER_PROFILE.md`，使用以下模板：

```markdown
# 项目用户画像

## 当前开发者偏好

- 沟通语言: Chinese
- 工作风格: （根据观察填写，如"先高确定性组件，评估结果，再设计不确定部分"）
- 文件命名: （根据观察填写，如"yyyymmdd-hhmm 后缀"）
- 质量要求: （根据观察填写）
- 记忆管理: 自动保存，旧记忆保留，新记忆带时间戳

## 贡献者日志

| 时间 | 开发者 | 设备标识 | 会话ID | 变更摘要 |
|------|--------|---------|--------|---------|
| YYYY-MM-DD HH:MM | <开发者名> | <设备标识> | <session_id> | <变更摘要> |

## 设备历史

### <设备标识> (<开发者名>)
- 首次使用: YYYY-MM-DD
- 最后使用: YYYY-MM-DD
- 工作内容: ...

### [新设备 clone 后自动追加]
- 首次使用: YYYY-MM-DD
- 最后使用: YYYY-MM-DD
- 工作内容: ...
```

**设备标识获取方式**：
- Windows: `%COMPUTERNAME%`（设备名）+ `%USERNAME%`（开发者名）
- macOS/Linux: `hostname`（设备名）+ `$USER`（开发者名）

**自动追加逻辑**：
- 每次会话开头检查时，读取 USER_PROFILE.md 的设备历史
- 如果当前设备不在列表中 → 追加新设备条目
- 如果当前设备在列表中 → 更新"最后使用"时间

## sessions/MD 模板（v2.0 新增）

`dev-memory/sessions/YYYY-MM-DD.md` 格式：

```markdown
# 会话摘要 — YYYY-MM-DD

## HH:MM — <一句话标题> [session: <session_id>]
<2-4 句话摘要，概括本次会话做了什么、得出什么结论>
[续: <上次相关会话的日期和标题>]

## HH:MM — <一句话标题> [session: <session_id>]
<2-4 句话摘要>
[续: <上次相关会话的日期和标题>]
```

**设计要点**：
- 每日一个文件，记录当天所有会话的摘要
- 每个会话摘要 2-4 句话
- 包含 session_id 便于关联 JSONL
- `[续: ...]` 标注跨会话连续性，链接到上一次相关会话
- 按时间正序排列（最早在上，最新在下）
- 如果当日文件不存在，创建新文件并写入头部

## sessions/JSONL Schema（v2.0 新增，必选）

`dev-memory/sessions/YYYY-MM-DD.jsonl` 每行一个 JSON 对象，9 个字段完整说明：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `message_id` | string | 是 | 消息唯一标识（取 session_id 前 12 位 + 序号，或使用消息 hash） |
| `timestamp` | string | 是 | 精确到分钟，格式 `YYYY-MM-DD HH:MM` |
| `session_id` | string | 是 | 会话标识（与 DEV_MEMORY.md metadata 中的 session_id 一致） |
| `intent` | string | 是 | 用户意图/请求（1 句话概括） |
| `actions` | string[] | 是 | AI 执行的动作列表（每个动作为一个字符串元素） |
| `outcome` | string | 是 | 结果（1-2 句话） |
| `learned` | string[] | 否 | 学到的新知识/经验（每个知识点一个元素） |
| `dimensions` | string[] | 否 | 关联的六维维度编号（①~⑥） |
| `references` | string[] | 否 | 引用的 DEV_MEMORY.md 已有条目（格式：`"DEV_MEMORY.md ⑤条目: 条目摘要"`） |

**完整 JSON 示例**：

```json
{"message_id":"6a55a2eef0e6","timestamp":"2026-07-14 10:52","session_id":"6a55a2eef0e645bb36d55adf","intent":"用户要求创建公开仓库","actions":["检查私有仓库记忆文件状态","创建 orphan 分支","推送至 dev-memory-public"],"outcome":"公开仓库创建成功，私有仓库保留记忆历史","learned":["orphan 分支可以创建无历史记录的干净初始提交","git rm --cached 可取消已跟踪文件的追踪"],"dimensions":["①","⑤"],"references":["DEV_MEMORY.md ⑤条目: 隐私感知Git策略"]}
```

**写入规则**：
- 如果当日文件不存在，创建新文件
- 追加到文件末尾，一行一条，不以逗号分隔
- 每次"任务完成"硬触发时追加一条
- 每次"话题切换"硬触发时追加一条
- 分诊跳过时不写入
- `learned`、`dimensions`、`references` 为空时使用空数组 `[]`，不可省略字段

## 溢出处理（v2.0，替代归档）

当主文件 DEV_MEMORY.md 某维度条目超出上限时，**不再移动到归档文件**，而是采用标记 + Dream 合并策略。

**溢出上限（v2.0 调整）**：
- 任务摘要 / 踩坑记录：上限 **8 条**（v1.3 是 5 条，因不再归档，适当放宽）
- 探索路径 / 架构决策：上限 **5 条**（v1.3 是 3 条）
- 最终结论 / 环境信息：上限 **8 条**（v1.3 是 5 条）

**超出上限的处理流程**：

1. **标记 `[~]`**：在超出上限的旧条目（位于各维度区块底部的）前标记 `[~]`，表示"已溢出，详情见 sessions/"
   ```markdown
   - [~][2026-06-15 14:30] [★] 旧条目内容 [引用:0]
   ```

2. **Dream 整理优先合并**：在 Step 6.5 Dream 整理时，优先扫描所有标记 `[~]` 的溢出条目，执行合并/去重操作：
   - 如果溢出条目与较新条目内容相关 → 合并，标注 `[已合并→新条目]`
   - 如果溢出条目之间近似重复 → 去重，保留更详细的

3. **长期未引用标记**：如果溢出条目长期未被引用（>30 天），在标记前追加 `[~⚠️]`，提示"已溢出且低引用，详情见 sessions/"
   ```markdown
   - [~⚠️][2026-05-10 09:00] [★] 很旧的条目内容 [引用:0]
   ```

4. **不物理删除**：溢出条目始终保留在主文件中（以 `[~]` 或 `[~⚠️]` 标记），完整的原始记录在 `sessions/YYYY-MM-DD.jsonl` 中可查

**与 v1.3 归档机制的区别**：
- v1.3：超出上限的条目移动到 `archive/YYYY-Www.md` 按周归档文件
- v2.0：超出上限的条目以 `[~]` 标记保留在主文件，Dream 整理时优先合并，不移动到其他文件
- 理由：有了完整的消息级 JSONL 记录（sessions/），归档文件的存在价值降低；标记 + Dream 合并更简单，且保持主文件完整性

## 首次初始化特殊规则（v2.0）

当项目没有 `dev-memory/DEV_MEMORY.md` 时，执行首次初始化：

1. 创建 `dev-memory/` 和 `dev-memory/sessions/` 目录（v2.0 路径更新，无 `archive/`）
2. 创建 `dev-memory/DEV_MEMORY.md`（使用 DEV_MEMORY.md 模板）
3. 创建 `dev-memory/USER_PROFILE.md`（使用 USER_PROFILE.md 模板，v2.0 新增）：
   - 获取当前设备标识（Windows: `%COMPUTERNAME%`；macOS/Linux: `hostname`）
   - 获取当前开发者名（Windows: `%USERNAME%`；macOS/Linux: `$USER`）
   - 填入"设备历史"第一个条目和"贡献者日志"第一行
   - "当前开发者偏好"根据本次会话观察初步填写
4. 创建 `dev-memory/CONFIG.md`（使用 CONFIG.md 模板）
5. 如果 TRAE 内置 memory 中有该项目的 `project_memory.md`，将其内容作为初始记忆来源
   - 路径：`c:\Users\<用户>\.trae-cn\memory\projects\<project_path>\project_memory.md`
   - 将其中的"硬约束"归入"最终结论与最佳实践"
   - 将其中的"工程规范"归入"架构决策记录"
   - 将其中的"经验教训"归入"踩坑记录"和"最终结论"
6. 如果有 `topics.md`，将最近的 topic 摘要归入"任务与变更摘要"
7. 所有条目统一加上 `[YYYY-MM-DD HH:MM]`、价值标记 `[★★★/★★/★]` 和 `[引用:0]` 初始标记
8. 在"当前状态概览"中写明"首次初始化，基于 TRAE 内置 memory 迁移"（如有）
9. 创建当日 `dev-memory/sessions/YYYY-MM-DD.md` 和 `dev-memory/sessions/YYYY-MM-DD.jsonl`，写入首次初始化的会话摘要和 JSONL 记录

**主动初始化**：当 dev-memory skill 被加载到上下文时（或 Rule 注入生效时），如果开头检查发现当前项目没有 DEV_MEMORY.md，在本轮对话的结尾触发时主动初始化，不等用户要求。

## 迭代记录

> 此区域记录 dev-memory skill 自身的改进历程。
> 每次执行 Step 6 自我进化检查时，如发现改进点则追加于此。

### v1.0 — 初始版本 [2026-07-10 17:19]
- 六维记忆提取：任务摘要、踩坑记录、探索路径、最终结论、架构决策、环境信息
- 主文件 + 按周归档机制（ISO周数）
- 追加式更正，旧记忆不删除
- 触发时机：对话结束时自动执行
- 首次初始化支持从 TRAE 内置 memory 迁移
- 溢出阈值：摘要/踩坑/结论/环境 5条，探索/决策 3条

### v1.1 — 多项目识别与记忆分发 [2026-07-10 18:05]
- 改进内容：新增 Step 0（多项目识别与记忆分发），支持一个 workspace 内多项目、一次会话跨多项目的场景
- 触发原因：实际使用中发现一个 workspace 内经常有多个项目，一次会话可能同时涉及多个项目（如把 A 项目的接口给 B 项目用），原来只写入当前目录的记忆会导致其他项目的记忆遗漏
- 影响范围：执行流程从 6 步变为 7 步，Step 1~5 对每个涉及的项目分别执行一遍；核心原则新增第 5 条"多项目感知"
- 关键设计：按文件路径、项目名称、代码特征、已有记忆关联等线索判断归属；跨项目内容在各方均写入并标注关联；无法归属的内容写入 workspace 级通用记忆；新项目自动纳入

### v1.2 — 可靠性与效率优化 [2026-07-10 18:30]
- **双触发机制**：开头检查（每轮首次响应时静默扫描项目记忆状态）+ 结尾触发（任务完成/话题切换/显式请求/安全网/收尾语），解决"自动保存技能自己没保存记忆"的根本问题
- **硬触发规则**：从模糊的"收尾语"改为明确的客观事件（任务完成、话题切换），不依赖用户说特定的话
- **分诊机制**：新增 Step 1 分诊，不值得保存的对话直接跳过，避免无效 IO（约 200 token 分诊 vs 8000+ token 完整执行）
- **分层读取**：开头检查只读 metadata+概览（~100 token），对比检查只读最新条目（~500 token），完整执行才读全量（~2000 token）
- **增量写入**：Step 5 改为 SearchReplace 精确插入，只输出新条目+插入位置，不重写未变化部分（写入 token 从 ~2000 降到 ~200）
- **价值标记**：每条记忆标注 ★★★/★★/★，归档时低价值优先，高价值尽量保留
- **时效性检测**：>14 天未更新的维度标记"可能过时"，对比检查时主动验证旧记忆有效性
- **会话去重**：metadata 中记录 session_id，开头检查时对比，避免同一会话重复保存
- **自我审计**：Step 7 新增触发可靠性审计（触发来源/记忆缺口/首次初始化遗漏检测）
- **可见反馈**：保存完成后输出一行确认，创造可监督性
- **主动初始化**：skill 被加载时即检查，不等用户要求
- **成本收益**：SKILL.md 增长 ~1.5KB，但分诊+增量写入的节省使整体 token 消耗净降
- 执行流程从 7 步变为 8 步（新增 Step 1 分诊、Step 8 可见反馈）

### v1.3 — Dream 整理 + 多工具适配 + 隐私感知 + 健康度 [2026-07-10 19:15]
- **轻量 Dream 整理**（Step 7.5）：借鉴 Claude Code autoDream 三操作（合并/去重/提升），内联执行不需要 daemon，触发条件：>20条或>7天或>5更正
- **多工具适配**：新增 adapters/ 目录，支持 10 个编程工具（TRAE、Claude Code、Codex、Cursor、Copilot、Windsurf、Cline、CodeBuddy、OpenCode/MiMoCode、通用）
- **CONFIG.md**：项目内配置文件，含安装引导（多工具安装表）和隐私设置，始终纳入 Git
- **隐私感知**：记忆文件默认 gitignore，公开仓库忽略/私有仓库建议追踪，首次初始化时引导选择
- **健康度评分**：0-100 分，5 维度（新鲜度30%+覆盖度25%+密度20%+准确度15%+价值分布10%），在可见反馈中展示
- **记忆指纹**：metadata 行增加指纹摘要（6维/N条/M更正/分数），便于 AI 快速判断记忆价值
- 执行流程从 8 步变为 8+0.5 步（新增 Step 7.5 Dream，Step 8 增强）

### v2.0 — 三层混合分层 + 寄生式注入 + 消息级JSONL + 用户画像 [2026-07-14 11:00]
- **三层混合分层架构**：项目级索引（DEV_MEMORY.md，策展层）+ 会话级摘要（sessions/YYYY-MM-DD.md，导航层）+ 消息级记录（sessions/YYYY-MM-DD.jsonl，原始层，必选）。横向补充 USER_PROFILE.md（用户画像+贡献者日志+设备历史）和 CONFIG.md（配置+工具注入指引）
- **寄生式注入**：将触发规则以"薄 Rule"形式注入到各编程工具最高优先级层（TRAE Rules/CLAUDE.md/.cursor/rules/AGENTS.md/.windsurf/rules/.clinerules/copilot-instructions.md），厚 Skill（本 SKILL.md）按需加载。解决 v1.3 中 Skill 优先级最低导致触发不可靠的根本问题
- **消息级 JSONL 必选**：每次硬触发追加一条 JSONL 记录到 sessions/YYYY-MM-DD.jsonl，9 个字段（message_id/timestamp/session_id/intent/actions/outcome/learned/dimensions/references），结构化捕获每条消息的 intent/actions/outcome/learned
- **会话级 MD 摘要**：sessions/YYYY-MM-DD.md 提供人类可读的会话索引，每个会话 2-4 句话摘要 + session_id + 跨会话连续性标注
- **USER_PROFILE.md**：项目级用户画像（开发者偏好）+ 贡献者日志（时间/开发者/设备/会话/变更）+ 设备历史（跨设备溯源）。新设备 clone 项目后自动追加设备条目，实现跨设备记忆继承闭环
- **取消按周归档**：移除 archive/ 目录和 ISO 周数机制，溢出条目以 `[~]` 标记保留在主文件中，Dream 整理时优先合并。理由：有了完整的消息级 JSONL 记录，归档文件的存在价值降低
- **引用计数**：每条记忆末尾增加 `[引用:N]` 标记，记录被后续会话引用的次数。Dream 整理时根据引用频率动态提升价值等级（★≥3次→★★，★★≥5次→★★★，★★★>30天未引用→标记低引用）
- **跨层合并**：Dream 整理新增跨层操作，扫描 sessions/JSONL 中的 learned 字段，被多次独立记录的知识提升为 DEV_MEMORY.md 的 ④ 最终结论
- **核心原则调整**：从 10 条调整为 12 条。第 7 条"零依赖"修改为"纯 Markdown + JSONL"；新增第 11 条"三层混合分层"；新增第 12 条"寄生式注入"
- **执行流程调整**：从 8.5 步简化为 7.5 步。移除原 Step 6（溢出归档）；原 Step 5 拆分为 5.1（写入 DEV_MEMORY.md）+ 5.2（追加 JSONL）+ 5.3（更新会话摘要）+ 5.4（更新 USER_PROFILE.md）；原 Step 7→Step 6，原 Step 7.5→Step 6.5，原 Step 8→Step 7
- **路径迁移**：所有路径从 `docs/dev-memory/` 改为 `dev-memory/`（直接放在项目根目录）
- **溢出上限放宽**：任务摘要/踩坑记录 5→8 条，探索路径/架构决策 3→5 条，最终结论/环境信息 5→8 条（因不再归档，适当放宽）
- **metadata 更新**：移除 `周归档: YYYY-Www`，新增 `最后整理: YYYY-MM-DD`
- **开头检查增强**：新增设备检查（USER_PROFILE.md 设备历史）和 sessions/ 目录检查
- **Step 4 增强**：新增引用计数更新逻辑
- **Step 6.5 Dream 增强**：新增引用频率提升和跨层合并操作，触发条件从 >20 条调整为 >24 条
- **成本变化**：单次执行增加 ~800 token（+14%），但触发可靠性从"中"提升到"高"（质变），新增消息级完整记录、会话级索引、用户画像和设备溯源
